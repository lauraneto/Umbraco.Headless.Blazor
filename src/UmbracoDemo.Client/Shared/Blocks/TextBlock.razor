@if (Text is null)
{
    return;
}

<MudText>@((MarkupString)Text)</MudText>

@code {
    [Parameter]
    public TextBlock__cadElementModel? Content { get; set; }

    public string? Text => Content?.Properties.Text?.Markup;
}